console.log(1);
// ctrl + ` (백틱)을 사용하면 터미널 열림!
// 그 다음 아래 + 버튼을 클릭해 새로운 Commend Prompt(CMD) 창을 생성
// 그 후 터미널에 현제 디렉터리에 위치해 있는지 확인 후 node myfile.js 형식으로 실행 가능.
// npm init 명령어를 터미널에서 사용하면 json 프로젝트를 만들 수 있음.

function add(a, b) {
    return a + b;
}
console.log(`1 + 2 = ${add(1, 2)}`)